---
collections:
  # RouterOS API modules (api, api_modify, api_info, api_find_and_modify) and the
  # module_defaults action group group/community.routeros.api used in site.yml.
  # Pinned to the version this repo is tested against (see CHR rehearsal notes
  # in docs/bootstrap/README.md) rather than a floor, so a collection upgrade
  # cannot silently change api_modify's PK/path coverage under the roles.
  - name: community.routeros
    version: "3.21.0"
